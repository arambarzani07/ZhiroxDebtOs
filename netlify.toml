[build]
  command = "flutter config --enable-web && flutter pub get && flutter analyze --no-fatal-infos && flutter build web --release --base-href /"
  publish = "build/web"

[build.environment]
  FLUTTER_VERSION = "stable"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
